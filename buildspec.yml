version: 0.2

phases:
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
      - ls -ltr
      - chmod 755 invoke.sh
      - ls -ltr
      - /bin/bash invoke.sh 
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
